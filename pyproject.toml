[project]
name = "tinybinius"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "algebra",
]

[tool.uv.sources]
algebra = { git = "https://github.com/tinycrypto/algebra.git" }
